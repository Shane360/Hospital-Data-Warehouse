The Scripts for this layer are divided into 3 to create:

a. __Audit Log Table__: to capture all changes in this layer for audit purposes

b. __Staging tables__ to hold the raw, fully loaded data from the CV files (saved as bronze.stage_<table_name>)

c. __Historical tables__ to hold current data and merge new data from staging tables. These also indicate where changes are made
